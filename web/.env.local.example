# The frontend READS ONLY, against the select-only RLS policies in
# ops/supabase_live_log.sql. Both values below are public by design: they are
# compiled into the browser bundle by Next.js.
#
# NEVER put SUPABASE_SERVICE_KEY in this file. The service role bypasses RLS
# entirely (see section 5 of the DDL), and only scripts/phase6_*.py write.

NEXT_PUBLIC_SUPABASE_URL=https://jbshjoegahqniswwxwjk.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=
