===============================================================================
PHASE 4 - D2: DYNAMIC STATE
PRE-DECLARATION.  Written before D2 was fitted.
===============================================================================

STATUS: BLOCKED ON ONE DECISION (section 9). Nothing is fitted against this
document until that decision is made and recorded here as an amendment.

-------------------------------------------------------------------------------
0.  WHAT THIS RUNG IS FOR
-------------------------------------------------------------------------------

    The tier-2 experiment returned WITHIN-SEASON UPDATING:

        Delta_total    +0.0359  [+0.0186, +0.0535]
        Delta_recency  +0.0356  [+0.0177, +0.0533]
        Delta_sample   +0.0003  [-0.0007, +0.0014]
        share_recency  99.1%    (RPS 99.6%, signs agree)

    Recency is essentially the whole effect. So D2's state must genuinely
    update WITHIN the test season. A rung that refits only at season
    boundaries forfeits the measured effect and would be a different
    experiment wearing D2's name.

-------------------------------------------------------------------------------
1.  REFIT CADENCE - INHERITED, NOT INVENTED  (load-bearing)
-------------------------------------------------------------------------------

    Read out of phase2_poisson_dixon_coles.run_fold():

        ONE REFIT PER DISTINCT MATCH DATE.
        The fitting window is every match with date STRICTLY < that date.

    Not per gameweek, not weekly, not per match. Per distinct calendar date,
    strictly prior. 583 distinct dates across the 1,900 matches; 582 refits
    (the first date has no prior window).

    This is stated at this length because recency is 99% of the effect, so
    cadence is not an implementation detail. It is held FIXED across every
    rung D2-D4 and across the D2-DC diagnostic.

    Already implemented and verified in scripts/phase4_dc_passthrough.py:
    G1 asserts the generated state reproduces Phase 2's stored test-row
    lambda_home / lambda_away to 0.000e+00 on all 1,520 test rows.

-------------------------------------------------------------------------------
2.  FEATURES - DIFFERENCES ONLY, FOUR COLUMNS
-------------------------------------------------------------------------------

    rel_elo_diff           home Elo before match  -  away Elo before match
    rel_attack_diff        DC attack, refit per window, home - away
    rel_defence_diff       DC defence, refit per window, home - away
    expected_total_goals   lambda_home + lambda_away

    expected_total_goals is load-bearing and is NOT dropped for parsimony.
    Without it D2 cannot represent scoring ENVIRONMENT - the quantity that
    moves draw probability independently of the mismatch between the sides -
    and would trail DC for a reason unrelated to dynamic state.

    NOT INCLUDED, deliberately:
        home_elo_before / away_elo_before alongside rel_elo_diff. Levels plus
        their own difference is the redundancy this project removed earlier.
        The difference alone is kept.

    Dixon-Coles is the PRIMARY state source and Elo secondary: DC's advantage
    is recency-driven and the 107-day half-life is doing that work, whereas
    Elo's K = 20 is a coarser recency mechanism over the same information.

-------------------------------------------------------------------------------
3.  THE LADDER AND ITS EXACT WIDTHS
-------------------------------------------------------------------------------

    D0   base rate. No fit. Training-fold H/D/A frequencies.        0 features

    D1   phase1_backbone MINUS home_prev_season_source and
         away_prev_season_source                                   84 features

    D2   D1 + the 4 features of section 2                          88 features

    D3   D2 + frozen Phase 3 C_context, used exactly as built      100 features

    D4   D3 + frozen X_prior_composite (24) and X_availability (3)
         X_metadata (4) is NOT added                               127 features

    FEATURE count is not DESIGN-MATRIX width. Four columns are categorical
    (kickoff_hour_bucket, day_of_week, home_prev_season_status,
    away_prev_season_status) and expand to declared indicator levels. The
    design width per rung is therefore ASSERTED AT RUNTIME by DS3 rather than
    written here as a number that could be wrong. DS3 also asserts strict
    nesting D1 < D2 < D3 < D4.

-------------------------------------------------------------------------------
4.  INNER VALIDATION - AND A CONFLICT IN THE PHASE 4 PRE-DECLARATION
-------------------------------------------------------------------------------

    Section 7 of outputs/phase4_dynamic_state_predeclaration.txt requires BOTH

        (a) blocks defined as contiguous (season, matchweek) blocks, and
        (b) every validation date STRICTLY later than every training date,
            asserted programmatically as DS12

    THESE ARE INCOMPATIBLE IN THIS DATASET and DS12 would fail. This is not a
    new discovery; it is Instrument 4's Amendment 1, re-measured here on the
    Phase 4 training rows:

        (season, matchweek) training blocks spanning over 7 days:  91
        worst case: 2022-23 matchweek 8 spans 2022-09-16 to 2023-04-04

    Postponed fixtures keep their ORIGINAL matchweek number, so a matchweek is
    not an interval of time and blocks built from it overlap in date.

    RESOLUTION, following Amendment 1: the atomic unit is the CALENDAR DATE.
    Everything else in section 7 is kept exactly:

        blocks         5 contiguous chronological segments of the outer
                       training rows, approximately equal match count
        inner folds    4, expanding: fold i trains on blocks 1..i and
                       validates on block i+1
        constraint     max(train date) < min(validation date), strictly,
                       asserted as DS12
        statistic      mean validation log loss pooled across the 4 folds
        tie-break      SMALLEST lambda among ties
                       (note: Phase 3 used LARGEST. Phase 4's rule governs.)

    FOLD 1'S 380 TRAINING MATCHES: the same construction applies unchanged -
    5 segments of roughly 76 matches, 4 expanding inner folds validating on
    roughly 76 matches each, pooling ~304 validation matches. No special case,
    because a scheme that changed shape at fold 1 would confound "lambda
    differs by fold" with "the selector differs by fold".

-------------------------------------------------------------------------------
5.  REGULARISATION
-------------------------------------------------------------------------------

    Grid, fixed and not extendable, 13 points:

        0.01, 0.03, 0.1, 0.3, 1, 3, 10, 30, 100, 300, 1000, 3000, 10000

    Selected INDEPENDENTLY per (rung, outer fold). Never globally, never using
    outer-test performance.

    lambda = 1000 from Phase 3 is NOT inherited. The rungs differ in width, so
    the penalty must be selected per rung to be fair.

    A selection landing on a grid boundary is REPORTED CLEARLY and the
    experiment STOPS rather than extending the grid.

    SOLVER DEVIATION, DECLARED: section 12 of the Phase 4 pre-declaration
    names lbfgs. This environment has no scipy. The solver used is the
    project's existing Newton method on the same penalised multinomial
    objective (phase3_ablation_ladder.fit_multinomial), which is strictly
    convex for lambda > 0 and therefore reaches the SAME unique optimum, to a
    declared gradient tolerance of 1e-9, deterministically. This is a
    different route to the same point, not a different model - but it is a
    deviation from the written text and is recorded as one.

-------------------------------------------------------------------------------
6.  STANDARDISATION AND INITIALISATION
-------------------------------------------------------------------------------

    Continuous features standardised to zero mean and unit variance using
    statistics fitted on TRAINING ROWS ONLY - and, inside the inner CV, on
    that inner fold's training rows only. Boolean and indicator columns pass
    through unstandardised.

    Inherited from Phase 2, unchanged:
        Elo   INITIAL_RATING 1500 flat for promoted and new teams;
              continuing teams regress to 1500 + (r - 1500) * 0.75
        DC    NEUTRAL_STRENGTH 1.0 for any team absent from the fitting window
        DC    MAX_GOALS 25, TIME_DECAY_HALF_LIFE_DAYS 107.0,
              RHO bounds -0.5 / 0.5, SCALING_TOLERANCE 1e-10

    PROMOTED-TEAM EXPOSURE, measured and reported per fold rather than
    assumed: the count of test matches in which either side takes a
    neutral/initial value because it is absent from the fitting window. These
    rows are NOT dropped; they are the rows where dynamic state has least to
    say and their count is the honest caveat on any D2 gain.

-------------------------------------------------------------------------------
7.  METRICS AND UNCERTAINTY
-------------------------------------------------------------------------------

    Full Phase 0 six-metric set. Log loss and RPS primary. Reported per fold
    and pooled over all 1,520 outer test matches.

    Paired per-match bootstrap on EVERY delta - D2-D1, D3-D2, D4-D3, and each
    rung against DC walk-forward. 10,000 draws, seed 20260901, 95% percentile.
    A bare delta is not a result.

    RPS must agree in SIGN with log loss on every comparison. A disagreement
    makes that comparison INCONCLUSIVE and it is reported as such.

-------------------------------------------------------------------------------
8.  THE TESTS
-------------------------------------------------------------------------------

    DS1   every feature value for a match derives only from matches with date
          STRICTLY earlier than that match's date
    DS2   no outer-test row enters any fit or any scaler, at any rung
    DS3   design widths and strict nesting D1 < D2 < D3 < D4
    DS4   corrupting an outer test season's FEATURES moves no selected lambda
          and no other fold's predictions (with the control that its own do)
    DS5   corrupting an outer test season's LABELS moves no selected lambda
    DS6   D0 reproduces Phase 2's frozen base rate to < 1e-9
    DS7   Block C and Block X columns are read from the frozen Phase 3
          artefacts and are byte-identical to them
    DS8   determinism: a second full run is bit-identical
    DS9   probability contract on every rung via the harness's own validator
    DS10  no frozen artefact moved hash; FROZEN_MANIFEST.txt verifies
    DS11  ANCHOR. The extracted DC state, fed through Phase 2's OWN
          outcome_probabilities(), reproduces dc_walkforward's 0.9904 pooled
          log loss to < 1e-9. This proves the state extraction is lossless
          BEFORE any logistic touches it, so a D2 shortfall cannot be blamed
          on extraction.
    DS12  every inner split satisfies max(train date) < min(validation date),
          strictly

-------------------------------------------------------------------------------
9.  THE OPEN DECISION - WHY THIS DOCUMENT IS BLOCKED
-------------------------------------------------------------------------------

    The DC-passthrough diagnostic ran and returned INVALID on a regime gate,
    not on the link. The finding:

        Phase 2's walk-forward only ever fits windows of at least one full
        training season, and only ever PREDICTS test-season matches. Applying
        the same rule to TRAINING rows enters a regime Phase 2 never did.

        lambda_home reaches 47.26 on a 19-match window (West Ham, 2021-08-23)
        and 21.94 on a 20-match window, against a maximum of 4.64 across all
        1,520 test rows. Dixon-Coles strengths are not identified on a
        fortnight of football.

        Those outliers sit in the TRAINING rows, so they distort the
        standardiser the test rows are then mapped through. That is what
        produced fold 2's passthrough log loss of 27.09 - not the logistic
        link. Reading that number as a link measurement would have been a
        false finding that reshaped the whole Phase 4 design.

        Exactly 380 training rows - the whole of 2021-22 - are fitted on
        windows smaller than the smallest window any test row was scored on.

    THE DECISION REQUIRED, and it must be made before any fit:

        A. BURN-IN BY SEASON. D2's training rows begin at 2022-23; 2021-22
           supplies state but is never a training row. Costs fold 1 its entire
           training set, so fold 1 would have to be dropped or redefined -
           which changes the frozen fold structure and is therefore a serious
           step.

        B. MINIMUM-WINDOW THRESHOLD. Training rows require a fitting window of
           at least N matches. N must be declared before fitting, not chosen
           from the failure. A principled, untuned choice is N = 380, one full
           season, which is also the smallest window any test row has - it
           makes training and test regimes identical by construction.

        C. WINSORISE / CLIP the state features at bounds fitted on training
           rows only. Keeps every row but changes the feature definition, and
           the bound is a new free parameter.

        D. Something else.

    RECOMMENDATION: B with N = 380. It is the only option whose threshold is
    fixed by the experiment's own structure rather than by the observed
    failure - it equalises the training and test regimes by definition - and
    it keeps all four frozen folds intact, which A does not.

    NOT CHOSEN HERE. Choosing a burn-in threshold having already seen which
    value broke fold 2 is precisely what this project's pre-declarations exist
    to prevent. The decision is recorded as an amendment before anything is
    fitted.

===============================================================================
END OF PRE-DECLARATION
===============================================================================


===============================================================================
AMENDMENT 2 - BURN-IN THRESHOLD, AND AN EXTENDED LAMBDA GRID
Written before the re-run. Section 9 is unblocked by this.
===============================================================================

A2.1  THE BURN-IN THRESHOLD IS 380, AND IT IS DERIVED, NOT TUNED

    G7 requires every training row to come from a fitting window at least as
    large as the smallest window any test row was scored on.

    That smallest window is 380 BY CONSTRUCTION, and the derivation needs no
    data at all:

        test rows are only ever scored inside a test season;
        every test season is preceded by at least one complete season;
        a complete season is 380 matches;
        therefore no test row is ever scored on a window below 380.

    From which the threshold follows as a fixed point, not a choice:

        below 380  admits training rows fitted on evidence no test row
                   ever sees - the model would be taught in a regime it is
                   never used in
        above 380  discards training rows whose regime the test set
                   genuinely contains - throwing away valid evidence

    380 is the only value that makes the training and test regimes identical.
    It was derivable before anything ran, and this amendment asserts it from
    the structure above rather than from any observed number.

    ON 47.26, AND WHY IT IS NOT AN INPUT HERE

    lambda_home reaching 47.26 on a 19-match window is the observation that
    prompted G7 to EXIST. It is cited for that, and for nothing else. It is
    not an input to the threshold's value, and the threshold would be 380 had
    the largest early lambda been 5 or 500.

    That distinction is the entire difference between this amendment and the
    pattern Phase 3's Amendment 2 fell into, where a rule was changed after
    the result was known and the outcome moved with it. Here the failure
    prompted a gate; the gate's threshold comes from the fold structure.

A2.2  THE COST, STATED PLAINLY

    Under the gate, 2021-22 contributes NO training rows - all 380 of its
    matches are fitted on windows below 380, most of them far below.

    Fold 1 trains on 2021-22 alone. So fold 1 has no training set under the
    gate and the DC-PASSTHROUGH DIAGNOSTIC RUNS ON FOLDS 2, 3 AND 4 ONLY.

    That is acceptable for this diagnostic and only this one: it answers a
    yes/no question about whether the logistic link preserves Dixon-Coles, and
    three folds and 1,140 test matches answer it.

    IT DOES NOT PROPAGATE TO D2. All four frozen outer folds remain in force
    for the ladder. If the burn-in gate turns out to bind on D2's own training
    rows, that is a separate question requiring its own declaration - it is
    NOT inherited from this amendment.

    CONSEQUENCE FOR THE COMPARISON FIGURE: dc_walkforward's 0.9904 is a
    four-fold number. A three-fold passthrough may not be compared against it.
    DC's figure is recomputed on folds 2-4 only, and every delta and interval
    in the re-run is against that recomputed figure.

A2.3  THE LAMBDA GRID IS EXTENDED DOWNWARD - DECLARED BEFORE THE RE-RUN

    Two of four folds selected lambda at the grid floor of 0.01. A floor
    selection means the grid was wrong for this feature regime, and Phase 4
    section 8 requires it be reported rather than absorbed.

    This is independent of the window problem and is fixed independently.

    NEW GRID, 21 points, half-decade spacing throughout, extending the
    existing 13 downward by four decades:

        0.000001, 0.000003, 0.00001, 0.00003, 0.0001, 0.0003,
        0.001,    0.003,
        0.01, 0.03, 0.1, 0.3, 1, 3, 10, 30, 100, 300, 1000, 3000, 10000

    The upper 13 are unchanged, so any earlier selection remains
    representable and the two grids agree wherever they overlap.

    A PREDICTION, RECORDED NOW SO IT CANNOT BE RETROFITTED

    The passthrough design carries exactly TWO features against 760 or more
    training rows. That is a regime in which the unpenalised maximum-likelihood
    fit is well posed and essentially optimal, so the inner CV has little
    reason to prefer any positive penalty. The floor may therefore continue to
    bind however far the grid is extended, and if it does that is NOT the
    pathology G6 was written to catch - it is the correct answer for p = 2.

    G6 is nevertheless left as a hard FAIL rather than softened. Per the
    brief, a floor selection after the G7 gate is a STOP-and-report, and the
    reading above is offered as an interpretation for that report, not as a
    licence to continue.

A2.4  SECTION 9 IS UNBLOCKED

    Option B is adopted at N = 380. Sections 1-8 are unchanged. Nothing else
    in this document moves.

===============================================================================
END OF AMENDMENT 2
===============================================================================


===============================================================================
AMENDMENT 3 - G6 IS SCOPED TO THE REGIME WHERE IT IS WELL POSED
Written before any re-evaluation. Applicability fixed for every planned rung.
===============================================================================

A3.0  DISCLOSURE, FIRST

    This amendment is written HAVING SEEN folds 3 and 4 of the DC-passthrough
    select lambda at the extended grid's floor of 1e-6. That is stated plainly
    here, exactly as Amendment 2 stated its relationship to 47.26, because an
    amendment written after a failure is the pattern this project polices
    hardest.

    The test that makes it legitimate is the same one: the applicability
    condition below must be derivable from the REGIME - from p and n and the
    conditioning of the fit - and must not reference which rungs failed. If it
    happens to exclude the passthrough, that must fall out of its two features
    against hundreds of rows, and would have fallen out identically had every
    fold selected lambda = 1000.

    It does. The condition is an external, pre-existing statistical criterion,
    imported rather than fitted, and its value for every planned rung is
    tabulated in A3.2 before any of them run.

A3.1  WHAT G6 IS ACTUALLY FOR, AND WHEN THAT QUESTION HAS AN ANSWER

    G6 exists to catch a MIS-SPECIFIED GRID: the data wants a smaller penalty
    than the grid offers, so the selected lambda is an artefact of where the
    grid was cut rather than a property of the data.

    That failure mode presupposes the penalty is doing work. If the
    unpenalised maximum-likelihood fit is itself well identified, then
    lambda -> 0 is not an artefact, it is the correct answer, and no extension
    of the grid will ever stop the floor binding because there is no interior
    optimum to find. In that regime G6 asks a question with no answer.

    So G6 is not failing. It is MALFORMED outside the regime it was written
    for, and this amendment gives it that regime explicitly.

    THE CONDITION: EVENTS PER VARIABLE

        EPV = (count of the RAREST outcome class in the rung's training rows)
              / (design-matrix width, excluding the intercept)

        G6 IS APPLICABLE   when EPV <  10
        G6 IS NOT APPLICABLE when EPV >= 10

    The threshold of 10 is the long-standing events-per-variable rule for
    logistic regression (Peduzzi et al., 1996, and the replication literature
    that followed it). It is the conventional dividing line below which an
    unpenalised logistic fit is unreliable - biased coefficients, unstable
    standard errors, separation risk - and above which it is considered well
    identified.

    THIS THRESHOLD IS IMPORTED, NOT FITTED. It predates this project, it was
    not chosen by looking at any number produced here, and it is not tuned to
    put the passthrough on either side of the line. The rarest class is used
    rather than n because the multinomial fit's precision is governed by the
    scarcest outcome - draws - not by the total.

A3.2  APPLICABILITY FOR EVERY PLANNED RUNG, FIXED IN ADVANCE

    Design width is the built design matrix excluding the intercept, so
    categorical expansion is counted, not the raw feature count.

      rung         fold   n_train   rarest   width      EPV   G6
      ---------------------------------------------------------------------
      passthrough  2          380       87       2    43.50   NOT APPLICABLE
      passthrough  3          760      169       2    84.50   NOT APPLICABLE
      passthrough  4         1140      262       2   131.00   NOT APPLICABLE

      D1           1          380       88      88     1.00   APPLICABLE
      D1           2          760      175      88     1.99   APPLICABLE
      D1           3         1140      257      88     2.92   APPLICABLE
      D1           4         1520      350      88     3.98   APPLICABLE

      D2           1          380       88      92     0.96   APPLICABLE
      D2           2          760      175      92     1.90   APPLICABLE
      D2           3         1140      257      92     2.79   APPLICABLE
      D2           4         1520      350      92     3.80   APPLICABLE

      D3           1          380       88     112     0.79   APPLICABLE
      D3           2          760      175     112     1.56   APPLICABLE
      D3           3         1140      257     112     2.29   APPLICABLE
      D3           4         1520      350     112     3.12   APPLICABLE

      D4           1          380       88     139     0.63   APPLICABLE
      D4           2          760      175     139     1.26   APPLICABLE
      D4           3         1140      257     139     1.85   APPLICABLE
      D4           4         1520      350     139     2.52   APPLICABLE

    The separation is not marginal. The LOWEST passthrough EPV is 43.50; the
    HIGHEST any D-rung reaches is 3.98. There is an order of magnitude between
    them and no rung sits near the threshold, so no rung's status turns on the
    exact value of 10 - a threshold of 5, or 20, would classify every row of
    this table identically.

    G6 THEREFORE REMAINS FULLY APPLICABLE TO D1, D2, D3 AND D4, AT EVERY
    FOLD. This amendment does not soften the gate for the ladder. It excludes
    exactly one thing: a two-feature diagnostic.

    A CORRECTION, RECORDED HERE

        The instruction that prompted this amendment described D2 as "4
        features against 1,140+ training rows". That is the DYNAMIC-STATE
        BLOCK, not the rung. Section 3 of this document defines D2 as D1 plus
        those 4 - 88 features, design width 92. D2's EPV is therefore 0.96 to
        3.80, not the ~285 that 4 features would give, and G6 stays applicable
        to it. Had the "4 features" reading been used, D2 would have been
        wrongly exempted from its own grid gate.

A3.3  THE AMENDED GATE

    G6a  APPLICABILITY. Compute and report EPV for every (rung, fold) before
         selection. Applicability follows from the table above and is not
         decided per result.

    G6b  WHERE APPLICABLE (EPV < 10). Unchanged from the original G6: a
         selection at either grid boundary is a FAIL and stops the rung.

    WHERE NOT APPLICABLE (EPV >= 10). A boundary selection is recorded as
    INFO, carrying the selected lambda AND the EPV that made it
    non-applicable, and the rung proceeds. It is never silently dropped.

    The grid is NOT extended further in the non-applicable regime. A2.3
    already established that extending it cannot help when there is no
    interior optimum; it only moves the floor.

A3.4  WHAT THIS DOES NOT DO

    It does not change any fitted number. The passthrough run is deterministic
    and its result is untouched by this amendment - only the gate status
    attached to it moves.

    It does not relax G7, which passes on its own terms.

    It does not exempt any ladder rung from anything.

===============================================================================
END OF AMENDMENT 3
===============================================================================
